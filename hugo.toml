baseURL = "https://nostalume.github.io/Nexus-Blog/"
title = "Nexus"
locale = "en-US"
defaultContentLanguage = "en"
enableRobotsTXT = true
ignoreLogs = ['warning-goldmark-raw-html']

[module]
  [[module.imports]]
    path = "github.com/adityatelange/hugo-PaperMod"

  [[module.imports]]
    path = "github.com/nostalume/Noteloom"

[params]
  description = "A static blog platform for everyone!"
  author = ["nostalgia", "creatoy"]
  showShareButtons = true
  showReadingTime = true
  showCodeCopyButtons = true
  showToC = true
  showBreadCrumbs = true

  [params.profileMode]
    enabled = true
    title = "Nexus"

    [[params.profileMode.buttons]]
      name = "Posts"
      url = "posts"

    [[params.profileMode.buttons]]
      name = "Tag"
      url = "tag"

    [[params.profileMode.buttons]]
      name = "Archives"
      url = "archives"

    [[params.profileMode.buttons]]
      name = "Search"
      url = "search"

  [[params.socialIcons]]
    name = "Github"
    url = "https://github.com/nostalume/Nexus-Blog"

  [params.fuseOpts]
    isCaseSensitive = false
    shouldSort = true
    location = 0
    distance = 1_000
    threshold = 0.4
    minMatchCharLength = 0
    keys = ["title", "permalink", "summary", "content"]

[outputs]
  home = ["HTML", "RSS", "JSON"]

[markup]
  [markup.goldmark]
    [markup.goldmark.extensions]
      [markup.goldmark.extensions.passthrough]
        enable = true
        [markup.goldmark.extensions.passthrough.delimiters]
          block = [['\[', '\]'], ['$$', '$$']]
          inline = [['\(', '\)']]

[taxonomies]
  tag = "tag"

[caches]
  [caches.images]
    dir = ":cacheDir/images"
